### Setup a New Project

1. `firebase init`
   - Add emulators, functions, firestore
2. `npm install`
3. In the firebase console, enable Authentication
   - Add X login options
4. Enable auth in x dev dashboard https://developer.x.com/
   - Get your consumer keys API Key and Secret and add it to firebase dashboard
   - Put the firebase callback URL into the developer tools
